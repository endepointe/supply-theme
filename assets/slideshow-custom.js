$(document).ready(function() {
  $('.slides-custom').slick({
    dots: false,
    infinite: true,
    slidesToShow: 1,
    speed: 400,
    adaptiveHeight: true,
    fade: true, 
    cssEase: 'linear' 
  });
});